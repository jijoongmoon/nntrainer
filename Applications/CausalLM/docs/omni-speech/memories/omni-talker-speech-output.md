---
name: omni-talker-speech-output
description: "Next-session starter for Qwen2.5-Omni speech OUTPUT (Talker codec LM + Token2Wav vocoder) — full spec, plan, and verification approach"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0bfecd2f-f705-494e-9a95-33ff10cccdb3
---

Speech output is the last unbuilt Qwen2.5-Omni piece (everything text/audio/image/video → text is DONE and HF-verified; see [[omni-audio-architecture]]). Two subsystems. Start with the Talker (tractable, reuses existing infra), then Token2Wav (large). All reusable infra (Qwen2 decoder graph, `layers/mrope_apply`, `layers/embedding_injection`, CausalLM run loop, the converters under `Applications/CausalLM/res/qwen2.5-omni/`) is on branch **`qwen25-omni-multimodal`** (committed 85ed8af04).

Checkpoint: `/home/jijoongmoon/WorkSpace1/.hf_cache/hub/models--Qwen--Qwen2.5-Omni-3B/snapshots/f75b40e3da2003cdd6e1829b1f420ca70797c34e`. HF source: `/home/jijoongmoon/.local/lib/python3.10/site-packages/transformers/models/qwen2_5_omni/modeling_qwen2_5_omni.py` (transformers 4.53). Weight prefixes: `talker.` (293 tensors), `token2wav.` (809).

## Phase 1 — Talker codec LM (tractable, verifiable first)

**STATUS (2026-06-13): Phase 1 DONE & HF-verified end-to-end.** nntrainer Thinker→Talker reproduces HF's codec token ids EXACTLY (127/127, greedy) — Stage A (decoder/converter, replay HF embeds), Stage B (thinker capture), Stage C (full pipeline) all pass. Stage A note retained below. New files: `models/qwen25_omni/qwen25_omni_talker_causallm.{h,cpp}` (subclass CausalLM; input0 = host-computed fused inputs_embeds [seq,2048] → `thinker_to_talker_proj` FC 2048→896 → 24 decoder blocks w/ mrope_apply on q/k + mha rope_theta=0 → output_norm → `codec_head` FC 896→8448 no bias); `res/qwen2.5-omni/talker_converter.py` (talker.bin DFS order: proj, per-layer gate-first, output_norm, codec_head; + codec_embed.bin [8448,2048] host lookup; config promotes talker_config, drops tokenizer_config.json so main.cpp doesn't mangle the prompt); `res/qwen2.5-omni/test_talker.py` (HF CPU-fp32 dump: thinker+talker greedy, token2wav stubbed as nn.Module, monkeypatch check_torch_load_is_safe for spk_dict on torch<2.6, hooks thinker_to_talker_proj for per-step inputs_embeds); `res/qwen2.5-omni/verify_talker_stageA.py`. Registered `Qwen25OmniTalker` in main.cpp + meson. Talker model dir: `models/qwen2.5-omni-3b-talker/`. Design: host computes the fused 2048-dim inputs_embeds (codec embed lookup + reply-part ADD) and feeds them as input0 — matches HF (whose prefill does NOT add codec embeds to codec_mask rows; fusion is an ADD not a scatter), graph needs no embedding/addition layer. Run with `stageA:<dump_dir>` (replays HF embeds). **Caveat: prefill must be row-by-row** — see [[omni-talker-batched-prefill-mha-bug]].

**Stage C impl (DONE):** Talker holds a `ThinkerForCapture` (subclass of Qwen25OmniCausalLM, defined in talker .cpp) built from `models/qwen2.5-omni-3b/` via nntr fields `thinker_model_path` + `thinker_nntr_config`=`nntr_config_fp32.json` (MUST use fp32, not the default Q4_0 config). Thinker `generateReply` (greedy, row-by-row prefill) reproduces HF's 16 reply tokens. **Capture gotcha (the crux):** `getTensor("output_norm"/"embedding0")` AFTER a forward returns GARBAGE — nntrainer's memory planner aliases activation buffers (output_norm's buffer becomes lm_head's logits, ~2e3 scale). FIX: expose them as graph-LEAF outputs via a custom **non-inplace** `CaptureTapLayer` ("capture_tap", copies input→fresh output, has incremental_forwarding) wired after embedding0 and output_norm with NO consumer; compile multi-output `model->compile(x, {lm_head, hidden_tap, embed_tap}, INFERENCE)` (lm_head FIRST so DFS weight order is unchanged; taps have no weights). `incremental_inference` then returns out[0]=logits, out[1]=hidden, out[2]=embed (fresh new[] copies, un-aliased). The core `IdentityLayer` is IN-PLACE so it does NOT work (output aliases the tapped buffer). Host assembles talker_inputs_embeds + thinker_reply_part stream (G+1 rows: positions P+1..L-1 then thinker_embed(text_eos 151861)/text_pad(151859)) + codec_embed.bin lookup; drives the verified talker loop. Captured embeds vs HF: mean|Δ| ~3e-3 (prefill)/2e-2 (steps); a few "massive activation" dims hit max|Δ|~50 but codec argmax is robust → ids match exactly. Run: `nntr_causallm <talker_dir> "<text prompt>"` (end-to-end) or `stageBC:<dump>` (HF ids) or `stageA:<dump>` (replay HF embeds). Verifiers: `res/qwen2.5-omni/verify_talker_stageA.py` / `verify_talker_stageC.py`.

**NEXT — Phase 2 (Token2Wav: codec ids → waveform):** see Phase 2 section below. DiT flow-matching + BigVGAN. The Talker now emits the codec ids Token2Wav consumes.

## Phase 2 — Token2Wav (codec ids → 24kHz wav) [IN PROGRESS, started 2026-06-13]
Full HF spec + nntrainer-infra mapping in the approved plan `/home/jijoongmoon/.claude/plans/purring-wondering-barto.md` (overwritten from Phase 1). **Decision: build BigVGAN first** (deterministic), then DiT; **ECAPA speaker encoder precomputed in Python (MVP)**.

**STATUS (2026-06-15): Phase 2A (BigVGAN) plan CONFIRMED+CORRECTED; steps 1 (snake_beta) + 2 (conv1d_transpose + convT core-bug fixes) DONE & ground-truth-validated.** A 5-dimension verification workflow ground-truthed every load-bearing fact against nntrainer core + HF + the restored dump → confirmed plan at `/home/jijoongmoon/.claude/plans/bigvgan-2A-confirmed.md` (supersedes the 2A part of purring-wondering-barto.md). **Implementation order:** (0,DONE) convT bugs C2+C2b; (1,DONE) snake_beta; (2,DONE) conv1d_transpose; (3,DONE) antialiased_snake; (4,DONE) bigvgan converter; (5,DONE-graph) full BigVGAN graph; (6,DONE) **Stage C PASS (full mel→wav vs HF: max_abs 4.39e-6)**. **Phase 2A (BigVGAN) COMPLETE** — `models/qwen25_omni/qwen25_omni_bigvgan.{h,cpp}` (Qwen25OmniBigVGAN, registered in main.cpp). initialize() builds the imperative graph (reuses the validated topology), host process_mel + vocode(mel→wav) + 24kHz PCM16 WAV writer + run(mel-file). Validated through the FULL app (config→initialize→load_weight→run): raw mel→wav vs HF wav.npy max_abs 2.3e-5 (within 1 int16 LSB, 0 samples >1 LSB). COMMITTED `0b313ad87`. Model dir `models/qwen2.5-omni-3b-bigvgan/` (bigvgan.bin + config.json + nntr_config.json, gitignored). Compile-time fixed mel_frames (default 128 via nntr_config) — DiT emits num_codes*2 frames; recompile per length for other utterances. NEXT: Phase 2B (DiT) + end-to-end 2C.

## Phase 2B — DiT (codec ids → mel) [SPEC CONFIRMED 2026-06-15, NOT YET IMPLEMENTED]
Confirmed plan: `/home/jijoongmoon/.claude/plans/dit-2B-confirmed.md` (5-dim verification workflow vs HF + checkpoint + dump; found 8 corrections). DiT = flow-matching transformer (codec ids + speaker cond → mel [1,80,128]) driven by a HOST RK4 loop over ONE compiled non-incremental graph; feeds BigVGAN. 360 F32 tensors `token2wav.code2wav_dit_model.`. **Dims:** hidden 1024, 22 blocks, 16 heads, head_dim 64, ff_inner 2048, codec_embed [8194,512] repeats=2, seq 128. **Critical corrections (would silently fail):** (C1) the 128-d cond slot = ECAPA spk_encoder(ref_mel) run IN-GRAPH (40 ECAPA tensors), the 192-d = precomputed `cond.npy` raw; HF arg names speaker_embedding/condition_vector are SWAPPED between sample() and forward(). (C2) attn mask = per-layer BOOLEAN block-diagonal (block_size 24; only L0/L20 look back 1 block, L10 looks ahead 1; other 19 strict block-diagonal) — not a uniform additive mask. (C3) AdaLN-Zero chunk-6 order = [shift_msa,scale_msa,gate_msa,shift_mlp,scale_mlp,gate_mlp] (shift-first); 2 NO-AFFINE LayerNorms/block (can't reuse layer_normalization — forces affine). (C4) norm_out (AdaLN-Final) chunk-2 = [scale,shift] (SCALE-first, opposite). (C5) RoPE = ADJACENT-PAIR rotate `(x0,x1)→(-x1,x0)` + INTERLEAVED cos/sin `[f0,f0,f1,f1,…]`, HEAD 0 ONLY → **mrope_apply/vision_rope CANNOT be reused** (they're half-split); need new `dit_rope`. (C6) CFG = ONE batch=2 forward (recommend two batch-1 forwards re [[omni-talker-batched-prefill-mha-bug]]); 36 ODE calls (RK4 9 intervals×4). combine 1.5·guided−0.5·null. (C7) config.json dit_config aliases (dim/depth/heads) IGNORED by HF; block_size/look_*/rope_theta(10000) ABSENT → hard-code. (C8) ff_inner 2048; CFG null branch uses codec_embed ROW 0 (drop_code zeros ids, NOT a zero vector) + ECAPA(zeros) + speaker=0. **Impl order:** converter → dit_rope → dit_attention(fork vision_attention, block mask) → dit_adaln_zero/_final → qwen25_omni_dit model + host RK4/CFG → Stage A (per-step velocity, needs NEW dump hooks — current dump only has final dit_mel) + Stage B (inject initial_state_full[:, :128], match dit_mel). **ECAPA**: precompute ecapa128_pos/neg in Python & inject for bring-up; port last. Sampler: sway time `[0,.0152,.0603,.134,.234,.357,.5,.658,.826,1.0]`, RK4 3/8-rule. Per the workflow synthesis, the IMPLEMENTATION is well-scoped engineering (Sonnet-suitable); reserve Opus depth for Stage-A RoPE/mask numeric debugging.

**DiT IMPLEMENTATION PROGRESS (2026-06-16, on Sonnet):**
- **Stage-A HF dump DONE:** `res/qwen2.5-omni/dump_dit_refs.py` → `/tmp/omni_dit_dump/` (one ode step at sway[1]=0.0152, apply_cfg). Captured (batched [2,...] cond/null rows): `x_in[1,128,80]`, `time_emb[1,1024]`, `ecapa_out[2,128]` (row0=ECAPA(ref_mel), row1=ECAPA(0) → the pos/neg cond to inject), `code_embed[1,128,512]`+`code_embed_uncond`, `rotary_0`(cos)/`rotary_1`(sin)`[2,128,64]`, `input_embed_out[2,128,1024]`, `block0_out[2,128,1024]`, `norm_out[2,128,1024]`, `proj_out[2,128,80]`(per-row velocity), `velocity_cfg[1,128,80]`. Validation ladder: input_embed_out (host concat+proj) → block0_out (1 full block) → velocity (per-step graph) → Stage B (RK4 → dit_mel).
- **ARCHITECTURE DECISION — decomposed (reuse FC + attention), NOT monolithic:** because nntrainer `multiply`/`Tensor::multiply` do NOT broadcast (`tensor.h` "does not support broadcasting"), AdaLN gating (`[B,1024]` gate broadcast over seq) and modulation can't be plain graph ops. So the block graph = reuse `fully_connected` (Wq/k/v/o, ff.0/ff.3, attn_norm.linear, norm_out.linear, proj_out) + `activation`(swish=SiLU on time, tanh_gelu in MLP) + `dit_rope` + `dit_attention` + two small custom helpers that do broadcast+no-affine-LN in C++:
  - **`dit_modulate`** (DONE): inputs (x[B,1,seq,1024], cond[B,1,1,M]); no-affine LN(eps 1e-6)·(1+scale)+shift; scale/shift = 1024-wide slices of cond at props `scale_off`/`shift_off`. Per-block attn: scale_off=1024,shift_off=0; ff: scale_off=4096,shift_off=3072; final norm_out (scale-first): scale_off=0,shift_off=1024.
  - **`dit_gate`** (DONE): inputs (residual, x, cond); out = residual + cond[gate_off:+1024]·x. Per-block gate_msa: gate_off=2048; gate_mlp: gate_off=5120.
  - **`dit_rope`** (DONE): adjacent-pair `(x0,x1)→(-x1,x0)` + interleaved cos/sin, HEAD 0 only (channels 0..63), rest pass through; inputs (x,cos,sin). cond/cos-sin chunk layout: per-block attn_norm.linear out [B,6144] order [shift_msa,scale_msa,gate_msa,shift_mlp,scale_mlp,gate_mlp].
- **Block graph wiring (per block, h=hidden, cond_i = FC_attn_norm(SiLU(time)) [B,6144]):** mod_a=dit_modulate(h,cond_i,1024/0) → q/k/v=FC(mod_a) → q=dit_rope(q),k=dit_rope(k) → a=dit_attention(q,k,v) → ao=FC_o(a) → h2=dit_gate(h,ao,cond_i,2048) → mod_f=dit_modulate(h2,cond_i,4096/3072) → f=FC_ff3(tanh_gelu(FC_ff0(mod_f))) → h3=dit_gate(h2,f,cond_i,5120). Final: cond_o=FC_norm_out(SiLU(time)); dit_modulate(h,cond_o,0/1024) → FC_proj_out → velocity.
- **STILL TODO:** `dit_attention` (fork `vision_attention` which already does masked FP32 SDPA from q/k/v inputs — swap window mask for per-layer block-diff mask; props block_size=24, look_ahead/look_backward; non-causal; head0 already roped). Then `qwen25_omni_dit.{h,cpp}` model (host: ECAPA precompute via dump's ecapa_out, codec gather+repeat_interleave, time sinusoid, cos/sin build, RK4 3/8-rule + CFG two-batch-1-forwards) + meson/reg. Then **converter AFTER graph built** (dump actual DFS weight order, align). Then Stage A/B. dit.bin = 318 graph weights (exclude 40 ECAPA + codec_embed + inv_freq → host).
- **Stage C VALIDATED (graph + converter order both correct on first try):** full graph `conv_pre(conv1d k7 p3 +bias) → for i 0..5 [ups{i}=conv1d_transpose(k=UP_K[i],s=UP_R[i],p=(k-s)/2,+bias) → 3 AMPBlocks → addition(3) → scale(1/3)] → activation_post(antialiased_snake) → conv_post(conv1d k7 p3 NO bias) → host clamp[-1,1]`. AMPBlock b (kernel RES_K[b]=[3,7,11]): 3 sub-blocks kk, each `res=a; a=antialiased_snake→conv1d(k=RES_K[b],dil=RES_D[kk]=[1,3,5],pad=dil*(k-1)/2)→antialiased_snake→conv1d(dil=1,pad=(k-1)/2)→addition([res,·])`. The 3 AMPBlocks wired addition(input_layers=[b0,b1,b2]) → DFS sibling order = converter b=0,1,2 order ⇒ MATCHES (no reorder needed). Validated via `spike_bigvgan.cpp` (full graph, loads bigvgan.bin, feeds processed_mel, per-stage bisect targets conv_pre/ups{i}/stage{i}/activation_post/wav).
- **NEW small layer:** `scale_layer.{h,cpp}` ("scale", elementwise ×const from `scale` property, NO weights → no DFS impact) for the ÷3 mean (operation layers carry no scalar; `addition` sums N inputs). COMMITTED: scale + converter (`token2wav_bigvgan_converter.py`) after Stage C. Spikes (spike_ups0/antialias/bigvgan + preps + proto_antialias.py) kept LOCAL (uncommitted).
- **bigvgan converter DONE (mechanics):** `res/qwen2.5-omni/token2wav_bigvgan_converter.py` → `bigvgan.bin` (449 tensors == checkpoint, 115443504 floats, byte-count matches exactly; convT (0,1)-transpose, plain conv [out,in,k]→[out,in,1,k] no transpose, conv_post NO bias, snake α/β raw log-domain). Output dir `models/qwen2.5-omni-3b-bigvgan/` (+ config.json). Emits the PLANNED DFS-from-output order; the 3 parallel-AMPBlock branch order + inter-layer order are NOT yet confirmed against the graph — Stage C (step 6) validates/aligns them. Tensor order written: conv_pre; per stage i 0..5: ups.i.0, then resblocks i*3+{0,1,2} each as [per kk 0..2: activations.2kk, convs1.kk, activations.2kk+1, convs2.kk]; activation_post; conv_post. NOT yet committed (order unvalidated).
- **antialiased_snake DONE:** `Applications/CausalLM/layers/antialiased_snake.{h,cpp}` (= HF TorchActivation1d: UpSample1d→SnakeBeta→DownSample1d). Does up/snake/down INLINE per-channel (no nntrainer grouped-conv): ratio=2, 12-tap Kaiser-sinc filter (cutoff .25, hw .3) BAKED as a static constexpr array (same filter for up & down); UP = replicate-pad(5,5)→scatter convT stride2→×2→slice[15:15+2T]; SNAKE(α,β); DOWN = conv stride2 over replicate-pad(5,6) via index-clamp (no explicit pad buffer). Holds α/β weights itself (the .act SnakeBeta), requested alpha-then-beta. snake_beta standalone layer is NOT used in the bigvgan graph (only antialiased_snake is) — it was the step-1 warmup. Python proto (`res/qwen2.5-omni/proto_antialias.py`) + C++ spike (`spike_antialias.cpp`/`_prep.py`) both reproduce HF activation_post from stage5 to ~1.1e-6.
- **COMMITTED so far:** `710c93877` (conv2d_transpose C2+C2b core fixes), `9f93fc319` (snake_beta + conv1d_transpose layers). antialiased_snake + spikes still uncommitted (local).
- **TWO nntrainer CORE BUGS in `nntrainer/layers/conv2d_transpose_layer.cpp`, both fixed (the conv2d_transpose layer is NOT wired into any meson test — `unittest_layers_convolution2d_transpose.cpp` absent from test/unittest/layers/meson.build, no golden data committed — so these went undetected):** (C2) OUTPUT WIDTH used `eff_k_height` not `eff_k_width` at 3 spots (col2im :74, im2col :154, finalize :274) → ups output short by (k−1). (C2b) the input-index BOUNDS check in col2im_transpose + im2col_transpose inner loops was `if (h>=H || w>=W)` where H/W are KERNEL dims (k_height=1,k_width=11) → silently dropped all input columns ≥ k_width; fixed to `h>=in_height || w>=in_width`. Both fixed; ups0 spike (conv_pre.npy→ups0, transposed weight+bias) now matches HF dump to **max_abs 1.79e-6**. Validates C2+C2b + conv1d_transpose wrapper + converter (0,1) transpose + bias broadcast + .bin [kernel,bias] load order. (Follow-up: wire the conv2d_transpose unit test + generate goldens to lock this in.)
- **conv1d_transpose DONE:** `Applications/CausalLM/layers/conv1d_transpose.{h,cpp}` — app LayerImpl wrapping core `Conv2DTransposeLayer` with H=1 (mirrors Conv1DLayer); type `"conv1d_transpose"`; padding via Padding1D explicit value (BigVGAN p=(k−s)//2 → 3/2/1). meson + parent dep registered. Spike harness `res/qwen2.5-omni/spike_ups0.cpp` + `spike_ups0_prep.py` (meson exe `spike_ups0`) reusable for later stages (1-layer ccapi model: input→layer, model->load(.bin), inference, compare to dumped .npy as raw float32).
- **snake_beta DONE:** `Applications/CausalLM/layers/snake_beta.{h,cpp}` (per-channel, FP32, `out=x+(1/(exp(β)+1e-9))·sin²(exp(α)·x)`; implements `forwarding` only — base `incremental_forwarding` delegates to it per `layer_devel.h:225`, which is also why core conv layers run under `incremental_inference`; requests alpha THEN beta so DFS bin loads in that order). Formula HF-verified (3 pts <1e-4); compiles+links. NOT validated bare vs `activation_post.npy` — snake is ALWAYS wrapped in TorchActivation1d anti-alias (C8), so its real validation is step 3.
- **`requestWeight` correct signature** (prior note was wrong): `requestWeight(const TensorDim&, Initializer, WeightRegularizer, float reg_const, float decay, const std::string& name, bool trainable=true, ...)`; canonical trainable=false weight = `requestWeight(dim, Initializer::ZEROS, WeightRegularizer::NONE, 1.0f, 0.0f, "name", false)` (`layer_context.h:212`, `centroid_knn.cpp:64`). NO trailing `,0,0` after name.
- **Other corrections:** total upsample **240×** (NOT 1440); convT weight PyTorch `[in,out,k]`→nntr `[out,in,1,k]` = transpose dims (0,1) then unsqueeze (plain Conv1d `[out,in,k]` needs only unsqueeze, NO transpose); AMPBlock = 3 sub-blocks (6 convs/6 snakes), stage=MEAN of 3 parallel AMPBlocks (÷3); resblock channels 768→24 (1536 only feeds ups0); SnakeBeta `exp()` UNCONDITIONAL (do NOT pre-exp in converter), 1e-9 on exp(β) only, no clamp; conv_post NO bias; all 449 bigvgan tensors F32; Kaiser β=4.6638 (derived), up/down share one 12-tap filter; process_mel host transform verified vs dump to 2.4e-7.
- **/tmp dumps are EPHEMERAL** (power-loss wiped them 2026-06-15; regenerated). Regenerate via: `test_talker.py` → `/tmp/omni_talker_dump/talker_codes.npy`, then `dump_token2wav_refs.py --talker_dump /tmp/omni_talker_dump` → `/tmp/omni_t2w_dump/`. **BOTH scripts MUST run with `--model_path <local snapshot dir>` + `HF_HUB_OFFLINE=1 HF_HOME=/home/jijoongmoon/WorkSpace1/.hf_cache`** — else `resolve_model_dir` re-downloads to `~/.cache` (fills the /home disk). Snapshot: `/home/jijoongmoon/WorkSpace1/.hf_cache/hub/models--Qwen--Qwen2.5-Omni-3B/snapshots/f75b40e3da2003cdd6e1829b1f420ca70797c34e`.
- **Ground truth DUMPED** (2026-06-13): `res/qwen2.5-omni/dump_token2wav_refs.py` ran HF full Token2Wav (CPU fp32, talker codes[:64] + Chelsie cond) → `/tmp/omni_t2w_dump/`: `mel.npy [1,80,128]`, `wav.npy [30720]` (upsample **240×** = 5·3·2·2·2·2, NOT 1440; 24kHz/10ms hop), plus per-stage `conv_pre, ups0..5, stage0..5, activation_post, processed_mel, dit_mel, initial_state_full, cond, ref_mel, codes`. This is the Stage C (BigVGAN) + Stage A/B (DiT) reference.
- **Resolved facts:** no weight_norm (plain conv weights). **ConvTranspose1d weight layout SPIKE resolved**: nntrainer `Conv2DTransposeLayer` weight is `[filter(out), in, kh, kw]` (`conv2d_transpose_layer.cpp:242`) but PyTorch ConvTranspose1d is `[in,out,k]` → converter must **transpose dims (0,1)** → `[out,in,1,k]` (plain Conv1d weight is `[out,in,1,k]`, NO transpose; Conv1D wraps Conv2D w/ H=1, kernel "1,k", pad "0,0,p,p" per `conv1d_layer.cpp`). Semantics still to confirm at Stage C (first ups output). snakebeta α/β loaded via `InitLayerContext::requestWeight(dim, Initializer::ZEROS, NONE,0,0, name, trainable=false)` then `getWeight` in forward (NO existing custom layer loads weights — mirror conv2d's requestWeight/getWeight; α,β are part of the .bin in DFS order). snakebeta: `x + sin²(exp(α)·x)/(exp(β)+1e-9)`, per-channel.
- **REMAINING 2A work** (each a file): (1) `layers/snake_beta.{h,cpp}` (2 per-channel weights, exp in fwd); (2) `layers/conv1d_transpose.{h,cpp}` (delegate to core Conv2DTranspose, H=1, pad=(k−stride)//2); (3) `layers/antialiased_snake.{h,cpp}` = TorchActivation1d: replicate-pad + depthwise Kaiser-sinc convT ×2 up → snakebeta → replicate-pad + depthwise Kaiser-sinc conv ×2 down (filter baked at finalize via kaiser_sinc_filter1d, k=12, cutoff .25, hw .3; UpSample pad/slicing modeling.py:3140-3162, Down 3165-3187) — **highest risk, prototype vs HF on one AMPBlock**; (4) `res/qwen2.5-omni/token2wav_converter.py` (bigvgan.bin — **DFS order is the catch**: conv stack + AMPBlock 3-residual-subblocks + 3-resblock-mean; build model graph & match converter order, verify per-stage); (5) `models/qwen25_omni/qwen25_omni_bigvgan.{h,cpp}` (conv graph: conv_pre→6×[conv1d_transpose + mean of 3 AMPBlocks]→activation_post→conv_post; host process_mel exp→dB→normalize; non-incremental run like `qwen25_omni_audio_encoder.cpp:317`; WAV out); (6) meson + reg; (7) Stage C verify (feed dumped mel, diff per-stage <1e-3). BigVGAN config: mel_dim 80, up_init_ch 1536, rates[5,3,2,2,2,2], up_kernels[11,7,4,4,4,4], resblock_kernels[3,7,11], dilations[[1,3,5]×3], conv_pre[1536,80,7]+bias, conv_post[1,24,7] NO bias. AMPBlock fwd (modeling.py:3290): per 3 sub: res=x; x=act1(x); x=conv1(x)(dilated); x=act2(x); x=conv2(x)(dil 1); x=res+x. Stage = mean of 3 AMPBlocks (÷3).
- HF Token2Wav runnable on CPU fp32 (loads full model ~20s; token2wav stub NOT needed here — real bigvgan used).

A Qwen2-style decoder that autoregressively emits codec tokens conditioned on Thinker output.
- Config (talker_config): hidden 896, 24 layers, heads 14, kv_heads 2, head_dim 64, inter 4864, q/k/v bias, gate/up/down SwiGLU, RMSNorm eps 1e-6, rope_theta 1e6, **mrope_section [16,16,0]** (differs from thinker [16,24,24]; w-section is 0). vocab/codebook 8448, embedding_size 2048.
- Weights: `talker.model.*` = standard Qwen2 decoder (same layout as the text model — reuse the gate-first DFS converter). Plus `talker.model.embed_tokens.weight [8448,2048]`, `talker.thinker_to_talker_proj.weight [896,2048]`+bias (Linear 2048→896), `talker.codec_head.weight [8448,896]` (no bias).
- Forward (HF modeling line 2341-2362): `inputs_embeds = codec_embeds(2048) + thinker_reply_part(2048)`; `talker_lm_input = thinker_to_talker_proj(inputs_embeds)` (→896); Qwen2 decoder; `logits = codec_head(hidden)` (→8448). So embed dim is 2048 (codec embed + thinker hidden ADDED), THEN projected to 896. Decoder runs at 896.
- `thinker_reply_part = cat(thinker_hidden_states[1:]) + cat(thinker_token_embeds[1:])` (HF generate line 3917): the Thinker's per-token last-hidden-state PLUS its token embedding, for the reply tokens, consumed one per talker step (line 2341-2343 peels [:, :1, :] each step).
- Token ids: codec bos=8293, eos=8294, pad=8292, mask=8296; text bos=151860, eos=151861, pad=151859.
- **Main new work vs existing infra:** the Thinker must expose per-step hidden states + token embeddings (current CausalLM run() only emits text). Capture the pre-lm_head hidden (output_norm output) and embedding0 output per generated token to build thinker_reply_part.
- Verify: HF full model `Qwen2_5OmniForConditionalGeneration.generate(..., return_audio=False or talker path)` OR manual (thinker forward w/ output_hidden_states → build thinker_reply_part → talker.generate) → compare codec token ids vs nntr. HF talker generate: lines 2204-2450 (forward), 3753-3930 (full generate, thinker→talker hand-off at 3898-3925).

## Phase 2 — Token2Wav (large; codec tokens → waveform)
Two models under `token2wav.`:
1. `code2wav_dit_model` — flow-matching DiT (codec tokens → 80-dim mel). Params: emb_dim 512, enc_dim 128, enc_emb_dim 192, enc_channels [256,256,256,256,768], head_dim 64, ff_mult 2, mel_dim 80, num_embeds 8193, repeats 2. Submodules: input_embed, text_embed, time_embed, rotary_embed, transformer_blocks (DiT w/ adaLN-style time conditioning), norm_out, proj_out. Needs an **iterative ODE / flow-matching sampler** (N denoising steps) — the expensive, novel part.
2. `code2wav_bigvgan_model` — BigVGAN vocoder (mel 80 → 24 kHz waveform). upsample_rates [5,3,2,2,2,2] (hop=240), upsample_kernel_sizes [11,7,4,4,4,4], upsample_initial_channel 1536, resblock_kernel_sizes [3,7,11], resblock_dilation_sizes [[1,3,5]]×3. Submodules: conv_pre, ups (ConvTranspose1d), resblocks (dilated Conv1d + **snake activation** — custom layer needed), conv_post, activation_post. Needs Conv1d/ConvTranspose1d (conv1d exists; ConvTranspose1d may need adding) + snake activation custom layer.
- Verify: DiT mel vs HF (fixed noise seed), then BigVGAN waveform vs HF, then end-to-end audio.

## Notes / gotchas
- mrope_section [16,16,0] for talker — reuse `mrope_apply` + host cos/sin builder with this section; for pure text/audio reply the 3 axes are sequential so it's ~1-D rope, but keep the section general.
- Token2Wav diffusion sampling is iterative (multiple forward passes) — plan a host-side ODE loop calling the compiled DiT graph per step (like the per-chunk audio encoder loop).
- ConvTranspose1d: check if nntrainer core has it (`grep -ri convtranspose nntrainer/layers`); if not, it's a new core/custom layer.
- Honest scope: Phase 1 is ~1 session; Phase 2 (DiT sampler + BigVGAN) is the largest single subsystem in this whole effort — budget accordingly and verify each sub-block against HF before composing.
