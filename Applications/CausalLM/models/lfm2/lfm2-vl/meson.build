# LFM2.5-VL — multimodal model (vision tower + LLM + multimodal projector).
# Each component is wired in below. Future additions (projector, integrated
# top-level model) become sibling subdir entries.
subdir('vision')
